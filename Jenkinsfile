pipeline {
    agent any
    stages {
        stage('Gradle') {
            steps {
                bat 'gradle --version'
            }
        }
        stage('Gradle') {
            steps {
                bat 'build'
            }
        }
    }
}